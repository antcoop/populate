<h1><a href="/owners">Owners</a> with <a href="/pets">Pets</a></h1>
{{#each owners }}
  <h2>{{this.name}} has {{this.petCount}} pets:</h2>
  <ul>
  {{#each pets}}
    <li>{{this.name}}</li>
  {{/each}}
  </ul>
{{/each}}